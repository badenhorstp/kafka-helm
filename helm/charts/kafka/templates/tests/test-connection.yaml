apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-cp-kafka.fullname" . }}-test-connection"
  labels:
    {{- include "ms-cp-kafka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-cp-kafka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
---
# Source: ms-cp-kafka/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "ms-cp-kafka-test-connection"
  labels:
    helm.sh/chart: ms-cp-kafka-0.1.0
    app.kubernetes.io/name: ms-cp-kafka
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['ms-cp-kafka:80']
  restartPolicy: Never
